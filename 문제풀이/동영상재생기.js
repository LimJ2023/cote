function solution() {
  console.log("34:33".split(":"));

  function calcTime(time, command) {
    const timeArr = time.split(":");
    command;
    let minuete = timeArr[0];
    let second = timeArr[1];

    let resultSec = (second + input) % 60;
    let addMin = Math.floor((minuete + input) / 60);

    minuete += addMin;
  }
  var answer = "";
  return answer;
}
solution();
